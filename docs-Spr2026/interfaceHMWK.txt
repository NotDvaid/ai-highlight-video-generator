Global CSS file:
interfaceSettings.css
Path: src/app/interfaceSettings.css

David implemented the following global variables:

--home-card-radius
Used on: feature cards on the home page (landing-page.tsx)

--home-feature-gap
Used on: feature grid spacing on the home page (landing-page.tsx)

Mohammed implmented the following:

Template of the frontend before any changes whatsoever, and changing the original main color being green black and white to yellow black and white (Green throughout the CSS replaced with yellow)

Assisting to creating the light/dark mode, and the about us page

"Impact Reels" text enlarged. Changed from text-lg font-semibold to text-2xl md:text-3xl font-bold across all four pages (landing, upload, processing, result) so it's proportionate to the logo
